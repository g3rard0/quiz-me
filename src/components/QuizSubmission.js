import React, { Component } from 'react';
import { Button, Modal } from 'semantic-ui-react';

const QuizSubmission = () => {
    return (
      <Button basic color='green'>Submit Answers</Button>
    );
}

export default QuizSubmission;
